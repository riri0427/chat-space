.message
  .upper-info
    %p.upper-info__user= message.user.name
    %p.upper-info__date= message.created_at.strftime("%Y/%m/%d %H:%M")
  %p.message__text= message.content if message.content
  = image_tag message.image, class: "message__text" if message.image